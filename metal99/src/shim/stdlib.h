/* Deliberately almost empty. See README.md - minimp3 includes this header and
 * uses nothing from it. Declaring more would invite dependence on more. */
#ifndef SHIM_STDLIB_H
#define SHIM_STDLIB_H
#include <stddef.h>
#endif
